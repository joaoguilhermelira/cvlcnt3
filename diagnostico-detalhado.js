const { execSync } = require('child_process'); const fs = require('fs'); console.log('?? DIAGN‡STICO DETALHADO'); try { const packageJson = JSON.parse(fs.readFileSync('package.json', 'utf8')); console.log('?? Scripts:'); if (packageJson.scripts) { Object.entries(packageJson.scripts).forEach(([nome, comando]) =  ${nome}: ${comando}`)); } else { console.log('? Nenhum script encontrado!'); } console.log('?? Dependàncias:'); if (packageJson.dependencies) { Object.entries(packageJson.dependencies).forEach(([nome, versao]) =  ${nome}: ${versao}`)); } } catch (error) { console.log('? Erro:', error.message); } if (fs.existsSync('app/page.tsx')) { console.log('? app/page.tsx existe'); } else { console.log('? app/page.tsx n∆o existe'); } if (fs.existsSync('node_modules/next')) { console.log('? Next.js instalado'); } else { console.log('? Next.js n∆o instalado'); } 
