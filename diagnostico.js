const { execSync } = require('child_process'); const fs = require('fs'); console.log('?? DIAGN‡STICO WINDOWS'); try { const nodeVersion = execSync('node --version', { encoding: 'utf8' }).trim(); const npmVersion = execSync('npm --version', { encoding: 'utf8' }).trim(); console.log(`? Node.js: ${nodeVersion}`); console.log(`? npm: ${npmVersion}`); } catch (error) { console.log('? Node.js n∆o encontrado'); } if (fs.existsSync('package.json')) { console.log('? package.json encontrado'); } else { console.log('? package.json n∆o encontrado'); } if (fs.existsSync('app')) { console.log('? pasta app encontrada'); } else { console.log('? pasta app n∆o encontrada'); } if (fs.existsSync('node_modules')) { console.log('? node_modules encontrado'); } else { console.log('? node_modules n∆o encontrado - Execute: npm install'); } 
